export type IconProps = {
  color?: string;
  className?: string;
  onClick?: () => void;
  stroke?: string;
  strokeWidth?: string;
  width?: number;
  height?: number;
};

export type TestimonialProps = {
  quote: string;
  name: string;
  location: string;
  rating: number;
};
